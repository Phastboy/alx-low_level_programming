/**
 * main - function that prints a string with printf function
 *
 * Return: 0 if success, non-zero if otherwise
 */

#include <stdio.h>

int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,");
  return (0);
}
